# ready-mart

running with python3, flask, wheel, and passlib.

sqlite3 should be installed by default in python3

```bash
$ python3 -m venv h/

$ . h/bin/activate

$ pip3 install wheel

$ pip3 install flask

$ pip3 install flask
```
